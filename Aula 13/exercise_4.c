#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{

    int rows = 0, columns = 0;

    printf("\nDigite o número de linhas que deseja ter em sua matriz: ");
    scanf("%d", &rows);
    printf("\nDigite o número de colunas que deseja ter em sua matriz: ");
    scanf("%d", &columns);

    

}