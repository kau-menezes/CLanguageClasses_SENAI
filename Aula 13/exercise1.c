#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    
        // char *name = malloc(50);
        // strcpy(name, "ROMEUZINHO");
        
        // printf("\n-- %s", name);

        // printf("\n%d -  %d", strlen(name), sizeof(name[0]));
        
        // printf("\n%d", strlen(name));

        // strcpy(name, "AMORA");

        // printf("\n-- %s", name);

        // printf("\n%d - %d", strlen(name), sizeof(name[0]));

        // printf("\n%d", strlen(name));

        // realloc(name, strlen(name));

        // printf("\n-- %d - %d", strlen(name), sizeof(name[0]));
    float num = 0;
    printf("fala ae kkk \n");
    scanf("%f", &num);
    printf("\no numero eh %2f", num);




}